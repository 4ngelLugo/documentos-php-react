# Web App in React and PHP

Web app for saving user data and documents on a mysql data base as well as a directory for branch offices and phone extensions of the employees

-Authentication for users previously registered by an admin role user